# Exemplos – Semana 04

| Arquivo | O que demonstra |
|---------|-----------------|
| `ArrayListExample.java` | Criação, inserção, iteração e remoção em `ArrayList`. |
| `HashMapExample.java` | Acesso por chave, `getOrDefault` e iteração em `HashMap`. |

