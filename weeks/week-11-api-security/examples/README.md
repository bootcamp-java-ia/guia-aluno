# Exemplos – Semana 11

| Arquivo | O que demonstra |
|---------|-----------------|
| `Produto.java` | Entidade exposta pela API. |
| `ApiApp.java` | Servidor HTTP simples com endpoints `GET` e `POST`. |

> Teste com `curl http://localhost:8080/produtos` antes de partir para o `POST`.

